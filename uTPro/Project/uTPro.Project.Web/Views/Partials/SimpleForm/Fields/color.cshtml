@* Color picker *@
@using uTPro.Feature.SimpleForm.Helpers
@model uTPro.Feature.SimpleForm.Models.FormFieldViewModel
@{
    var h = new FieldHelper(Model, ViewData);
}

@h.Label()
<input type="color"
       id="@h.FieldId"
       name="@h.Name"
       value="@(Model.DefaultValue ?? "#000000")" />
@h.Error()
