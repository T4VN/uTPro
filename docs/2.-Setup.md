---
layout: default
title: "Setup"
description: "Step-by-step setup guide for uTPro: domain configuration, database setup, uSync data import, and first run."
permalink: "/2-Setup/"
---

# 2. Setup

## 2.1 Setup Domain
In addition to the configurations used for Umbraco, we also provide other configurations for backoffice and language customization:

<img width="322" height="90" alt="Setup appsettings config" src="/screenshots/uTPro/setup-appsettings-config.png" />

If you want to use multi site or configure separate backoffice, please update the **hosts file** according to each operating system.

* For Windows: C:\Windows\System32\Drivers\etc\hosts)
* For MacOS and Linux: /etc/hosts

<img width="606" height="488" alt="Setup hosts file" src="/screenshots/uTPro/setup-hosts-file.png" />

---
## 2.2 Setup Project
Setting up uTPro is simple. Once you have downloaded the source code, you can open the project solution file in Visual Studio. We recommend using Visual Studio 2022 (or later).

<img width="260" height="138" alt="Setup Visual Studio solution" src="/screenshots/uTPro/setup-visual-studio-solution.png" />

Then follow the minimum requirements of the Umbraco CMS project:

* Database: uTPro ships configured for **PostgreSQL** (default), and also supports **SQL Server** and **SQLite**. Change the ConnectionString in `appsettings.json` with key **umbracoDbDSN** => create an **empty database** on the chosen engine, then use its name/connection in **appsettings**. See [4.8 Database Provider](/4-Configurations/#48-database-provider-postgresql--sql-server--sqlite) for the connection string and provider name of each engine.

<img width="177" height="80" alt="Setup database config" src="/screenshots/uTPro/setup-database-config.png" />

<img width="954" height="124" alt="Setup connection string" src="/screenshots/uTPro/setup-connection-string.png" />

* Target framework: .NET 10 (Changes depending on the Umbraco version in use — Umbraco 17 runs on .NET 10)

<img width="847" height="307" alt="Setup target framework" src="/screenshots/uTPro/setup-target-framework.png" />

---
## 2.3 Setup Data
The project has been integrated with data, and you can use **uSync to synchronize data** after installing Umbraco CMS.

<img width="800" alt="Setup uSync import" src="/screenshots/uTPro/setup-usync-import.png" />

In the "Everything" group, select "Import" and wait for the results, then refresh the page, return to the "Content" tab to check the results.

<img width="502" height="269" alt="Setup uSync result" src="/screenshots/uTPro/setup-usync-result.png" />

