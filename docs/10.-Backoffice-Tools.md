---
layout: default
title: "Backoffice Tools"
description: "Backoffice tools in uTPro: Block Preview, error pages, SEO Audit, File Manager, Job Monitor, Form Builder."
permalink: "/10-Backoffice-Tools/"
nav_order: 12
---

# 10. Backoffice Tools

> For: **Content Editor** — Backoffice tools: Block Preview, error pages, SEO Audit, File Manager, Form Builder.

This page covers the backoffice-facing tools bundled with or commonly added to uTPro.

## 10.1 Dashboard & Header App

The **uTPro Dashboard** (Content section → *uTPro* tab) and the **Header App** (top-right popover) ship with `uTPro.Feature.Dashboard`. They show the installed/latest version, an update check, site statistics, current-user details and recent activity/audit trail.

Full details: [6. Dashboard & Header App](/6-Dashboard/).

## 10.2 Error pages

uTPro returns proper HTTP status codes for errors (no soft-404s), using `UseStatusCodePagesWithReExecute("/error/{0}")` in the pipeline ([3.5](/3-Project-Structure/#35-middleware-pipeline-order)).

- **Content**: the **Page Error** node (`globalPageError`, see [8.6](/8-Global-Settings/#86-error-page)) provides the 404/500 body and can be excluded from indexing.
- **Behaviour**: 404 → the not-found page (also configurable per site via the Home node's **Page Not Found** field, `pageNotFound`); server errors → the error template with a 500 status.
- The response keeps the correct status code so search engines and monitoring see the real result.

## 10.3 Block Preview

uTPro integrates [Umbraco.Community.BlockPreview](https://marketplace.umbraco.com/package/umbraco.community.blockpreview) so editors see components rendered as they'll appear on the site, directly in the Block Grid editor.

- Configured in `Startup/BlockPreviewSetup.cs` with custom view locations.
- Components can detect preview mode and render a lighter output (CSS only, no JS) via `Context.Request.IsBlockPreviewRequest()` — see [9.7](/9-Developer-Reference/#97-block-preview-aware-components).
- `App_Plugins` and backoffice static assets are cached by the browser; **hard-refresh** the backoffice after editing them.

![Block Preview](/screenshots/uTPro/tools-block-preview.png)

## 10.4 URL Viewer (optional package)

`uTPro.Feature.UrlViewer` adds a **URL Viewer** section to the backoffice. Fetch any URL and see exactly what a search engine sees — redirect chain, response headers, HTML source, composite SEO score (0–100), on-page SEO analysis, social tags, technical SEO, content metrics, cloaking detection and more.

![URL Viewer](/screenshots/uTPro/tools-url-viewer.png)

📖 **Full documentation:** [uTPro.Feature.UrlViewer](/uTPro.Feature.UrlViewer/)

## 10.5 SEO Audit (optional package)

`uTPro.Feature.SEOAudit` is the site-wide companion to URL Viewer. It crawls every Content and Media URL, checks for broken links/images/resources, and produces composite SEO scores, health scores, prioritised issues, and CSV export — all in a dedicated backoffice section. Per-node audit tabs auto-scan when you open content.

![SEO Audit](/screenshots/uTPro/tools-seo-audit.png)

![SEO Audit - Content Node](/screenshots/uTPro/tools-seo-audit-content-node.png)

📖 **Full documentation:** [uTPro.Feature.SEOAudit](/uTPro.Feature.SEOAudit/)

## 10.6 File Manager (optional package)

`uTPro.Feature.FileManager` provides a full server file browser + media cleanup toolkit. Browse, upload, download, edit (Monaco editor), preview media, manage multi-root locations, and scan the media library for unused/broken/duplicate/orphaned files.

![File Manager](/screenshots/uTPro/tools-file-manager.png)

📖 **Full documentation:** [uTPro.Feature.FileManager](/uTPro.Feature.FileManager/)

## 10.7 Simple Form Builder (optional package)

`uTPro.Feature.SimpleFormBuilder` — a visual form builder with 19 field types, conditional visibility, file uploads, entry management, import/export, and public REST APIs. Used by the Contact Form component + Turnstile (see [8.4](/8-Global-Settings/#84-contact-form--cloudflare-turnstile)).

![Form Builder](/screenshots/uTPro/tools-form-builder.png)

📖 **Full documentation:** [uTPro.Feature.SimpleFormBuilder](/uTPro.Feature.SimpleFormBuilder/)

## 10.8 Job Monitor (optional package)

`uTPro.Feature.JobMonitor` surfaces every recurring background job in a Settings dashboard — timing, execution telemetry, estimated next run, and an authorized **Run now** action.

![Job Monitor](/screenshots/uTPro/tools-job-monitor.png)

📖 **Full documentation:** [uTPro.Feature.JobMonitor](/uTPro.Feature.JobMonitor/)

## 10.9 Audit Log (optional package)

`uTPro.Feature.AuditLog` — the missing audit log & content log viewer. Browse, search, filter, sort, and export every login, save, publish, move, and delete from the Settings section.

📖 **Full documentation:** [uTPro.Feature.AuditLog](/uTPro.Feature.AuditLog/)


## 10.10 Search Plus (optional package)

`uTPro.Feature.SearchPlus` enhances the built-in Examine search with **synonym expansion** and **diacritics-insensitive matching**. Manage synonym groups from a backoffice workspace under Settings.

![Search Plus](/screenshots/uTPro/search-plus-backoffice.png)

📖 **Full documentation:** [uTPro.Feature.SearchPlus](/uTPro.Feature.SearchPlus/)

## 10.11 Block Preview Toggle

`uTPro.Feature.BlockPreviewToggle` adds a per-property **"Preview Mode"** switch for Block Preview in the backoffice.

Each Block Grid / Block List property shows a full-width **Preview Mode** bar as the first row. Toggle it to switch how blocks render:

- **ON** — Full visual server-rendered preview (BlockPreview)
- **OFF** — Compact block card (icon + label + content type name)

### Key points

- Toggle is **per property** (keyed by property alias)
- State is remembered **per browser** (persists in `localStorage`)
- No settings or code changes needed — it works out of the box
- Requires `Umbraco.Community.BlockPreview` to be installed

### When to use OFF mode

- When editing pages with many blocks (faster rendering)
- When the preview CSS causes layout issues during editing
- When you just need to reorder blocks quickly

> After changing App_Plugins JS files, restart the site and hard-refresh the backoffice (DevTools → Disable cache).

## 10.12 CMS Login Screen Customization

`utpro-cms-screen` (bundled with `uTPro.Feature.Dashboard`) customizes the Umbraco **login/auth screen** with uTPro branding — orange accent colours, a custom favicon, day-of-week greetings, and a clean background without the default Umbraco curves.

No configuration is required — the customization is applied automatically. To revert to the Umbraco defaults, remove the `App_Plugins/utpro-cms-screen/` folder.

📖 **Full documentation:** [6. Dashboard — CMS Login Screen](/6-Dashboard/#66-cms-login-screen-customization)
